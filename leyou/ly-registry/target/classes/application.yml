server:
  port: 10086
spring:
  application:
    name: eureka-server   #注册的微服务名字
eureka:
  client:
    service-url:
      defaultZone : http://127.0.0.1:10086/eureka  #注册地址,如果不设置，会把自己也注册进去
    register-with-eureka: false
    fetch-registry: false
